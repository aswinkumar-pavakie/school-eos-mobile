# incidents

Status: not yet implemented.

Faculty and Hostel Warden incident/discipline reporting (severity, category,
description - severity determines server-side downstream escalation, not the
client). Principal/Vice Principal-level discipline case review and safety
oversight is web-only.

**Source**: API docs v4.0 Phase 9 · Incidents/Discipline/Safety/Emergency (30 endpoints, all `WEB` except Emergency which is `BOTH` - see `features/home` for the SOS/Emergency entry point shared across roles).
**Roles**: Faculty, Hostel Warden (reporting only).
