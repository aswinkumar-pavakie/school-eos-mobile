# assessment

Status: not yet implemented.

Faculty marks entry (server-enforced to subjects the teacher actually
teaches; out-of-range marks rejected server-side with 422
`SEMANTIC_RULE_VIOLATION`) and Parent marks/report-card viewing (PDF via
signed URL, never constructed client-side).

**Source**: API docs v4.0 Phase 3 · Assessment/Examination/Report Cards (38 endpoints, `WEB` for setup, marks-entry surfaced to Faculty per workflow doc); LLD v5.1 error taxonomy.
**Roles**: Faculty (entry), Parent (view).
